import ParentsHQContent from '@/components/parents/ParentsHQContent';

/**
 * Parents HQ Dashboard - For existing parents to manage their children
 * This route is for parents who are already set up and want to access their dashboard
 */
export default function ParentsHQDashboard() {
  return <ParentsHQContent />;
}